{
  "name": "canvas-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:engine": "cd engine && wasm-pack build",
    "build:engine:debug": "cd engine && wasm-pack build --debug",
    "serve": "vite",
    "build": "concurrently --kill-others-on-fail \"npm:type-check\" \"npm:build-only\"",
    "build-only": "vite build",
    "build:pages": "concurrently --kill-others-on-fail \"npm:type-check\" \"npm:build-only -- --base=/rust-canvas-test/\"",
    "type-check": "vue-tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.2.3",
    "concurrently": "^8.2.0",
    "sass": "^1.58.0",
    "vue": "^3.2.45",
    "vue-router": "^4.1.6"
  },
  "devDependencies": {
    "@tsconfig/node18": "^2.0.1",
    "@types/node": "^18.16.16",
    "@vitejs/plugin-vue": "^4.0.0",
    "@vue/tsconfig": "^0.4.0",
    "typescript": "~4.7.4",
    "vite": "^4.3.9",
    "vite-plugin-wasm": "^3.2.1",
    "vue-tsc": "^1.0.12",
    "wasm-pack": "^0.10.3"
  }
}
